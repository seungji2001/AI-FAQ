<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plateer.thingz.bo.mapper.FaqSumrInfoMapper">
    <insert id="insertMstGoods" parameterType="List">
        insert into faq_sumr_info(pgm_id, goods_id, live_start_end_id, start_date, end_date, link_status)
        values
            <foreach collection="list" item="item" separator=",">
                (#{item.pgmId}, #{item.goodsId}, #{item.id}, #{item.strtDt}, #{item.endDt}, 'W')
            </foreach>
    </insert>
</mapper>